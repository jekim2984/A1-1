# 항목 1. 기능 동작 검증

## Q1. Python 버전이 3.10 이상으로 확인되는가?
**답변:**
* **확인 방법:** 터미널에서 `python3 --version` 명령어를 입력하여 확인합니다.
* **현재 상태:** 시스템에 최신 **Python 3.12.13**이 설치되어 정상 작동하고 있습니다.
* **설명:** 이번 과제의 필수 제약 조건은 Python 3.10 이상이며, 현재 환경은 이를 완벽하게 충족합니다. VSCode에서도 우측 하단 인터프리터 설정이 Python 3.12로 잡혀 있습니다.

---

## Q2. Git 버전과 사용자 정보가 설정되어 있는가?
**답변:**
* **확인 방법:** 터미널에서 `git --version` 및 `git config --list` 명령어로 확인합니다.
* **현재 상태:** 
  * Git 버전: `git version 2.53.0`
  * 사용자 이름: `jekim2984`
  * 사용자 이메일: `jekim2985@gmail.com`
  * 기본 브랜치: `main`
* **설명:** Git 전역(Global) 설정에 과제 제출자의 식별 정보가 등록되어 있어, 모든 커밋에 작성자 정보가 올바르게 기록됩니다.

---

## Q3. GitHub 저장소에 코드가 업로드되어 있는가?
**답변:**
* **저장소 URL:** `https://github.com/jekim2984/A1-1`
* **현재 상태:** 로컬에서 작업한 필수 기능 안정화, 보너스 과제(JSON 저장, Markdown 내보내기, 조회수 카운트), 브랜치 병합 커밋까지 총 25개의 커밋이 원격 저장소(`origin/main`)에 모두 푸시되어 업로드 완료되었습니다.

---

## Q4. 샘플 저장소를 clone한 기록이 있는가?
**답변:**
* **확인 방법:** 터미널에서 `git reflog`를 입력하여 과거 Git 작업 내역을 확인합니다.
* **기록:** `HEAD@{0}: clone: from https://github.com/jekim2984/A1-1.git` 기록이 남아 있습니다.
* **설명:** GitHub에 생성한 원격 저장소를 로컬 컴퓨터로 `git clone` 명령어를 통해 내려받아 프로젝트 폴더를 구성하고 작업을 시작했습니다.

---

## Q5. 프로그램 실행 시 메뉴와 번호 입력이 가능한가?
**답변:**
* **실행 방법:** 터미널에서 `python3 prompt_manager.py`를 실행합니다.
* **원리 및 동작:**
  * 프로그램이 실행되면 `show_menu()` 함수를 통해 0번부터 11번까지의 기능 목록이 콘솔에 깔끔하게 출력됩니다.
  * `main()` 함수의 `while True` 반복문 안에서 `choice = input("선택: ").strip()`으로 사용자 입력을 대기합니다.
  * 사용자가 번호를 입력하면 해당 기능 함수가 실행되고, 종료 후 다시 메뉴로 복귀합니다.
  * 만약 메뉴에 없는 번호나 문자를 입력하면 `❌ 올바른 메뉴 번호를 입력해주세요.` 안내 메시지를 띄우고 프로그램을 종료하지 않은 채 메뉴를 다시 보여줍니다.

---

## Q6. 새로운 프롬프트를 추가하면 목록에 반영되는가?
**답변:**
* **동작 과정:**
  1. 메뉴에서 `1`번을 선택하면 `add_prompt()` 함수가 실행됩니다.
  2. 제목, 내용, 카테고리를 입력받습니다. (빈 문자열 입력 시 다시 입력을 요청하는 유효성 검사 적용)
  3. 입력된 데이터는 딕셔너리 형태로 묶여 전체 프롬프트 리스트(`prompts.append(...)`)에 즉시 추가됩니다.
  4. 동시에 `save_prompts()`가 호출되어 `prompts.json` 파일에도 영구 저장됩니다.
  5. 이후 `2`번(프롬프트 목록)을 선택하면 새로 추가된 프롬프트가 번호와 함께 즉시 표시됩니다.

---

## Q7. 카테고리별 조회가 되는가?
**답변:**
* **동작 과정:**
  * 메뉴에서 `3`번(`show_by_category()`)을 선택하면 정의된 카테고리 목록(텍스트 생성, 이미지 생성, 영상 생성, 페르소나, 자동화, 기타)이 번호와 함께 표시됩니다.
  * 원하는 카테고리 번호를 누르면 파이썬의 리스트 컴프리헨션을 사용해 해당 카테고리와 일치하는 프롬프트만 필터링(`[p for p in prompts if p["category"] == selected_category]`)하여 출력합니다.
  * 만약 해당 카테고리에 프롬프트가 하나도 없으면 `해당 카테고리에 등록된 프롬프트가 없습니다.`라는 친절한 안내 메시지가 출력됩니다.

---

## Q8. 검색은 어떻게 되는가?
**답변:**
* **동작 과정:**
  * 메뉴에서 `4`번(`search_prompt()`)을 선택하고 검색어 키워드를 입력합니다.
  * 파이썬의 `in` 연산자를 활용하여 제목 또는 본문에 검색어가 포함되어 있는지 검사합니다:
    `keyword in p["title"] or keyword in p["content"]`
  * 제목에 키워드가 있든 본문에 있든 모두 찾아내어 목록으로 보여주며, 찾은 개수(`총 N개의 프롬프트를 찾았습니다.`)를 알려줍니다. 일치하는 결과가 없으면 결과가 없다는 안내 메시지를 표시합니다.

---

## Q9. 즐겨찾기 추가/해제가 되는가?
**답변:**
* **동작 과정:**
  * 메뉴에서 `6`번(`toggle_favorite()`)을 선택하고 프롬프트 번호를 입력합니다.
  * 불리언 반전 연산(`not`)을 사용해 즐겨찾기 값을 토글합니다:
    `p["favorite"] = not p.get("favorite", False)`
  * 이미 즐겨찾기되어 있으면 해제되고, 해제되어 있으면 추가됩니다.
  * 즐겨찾기된 프롬프트는 목록에서 노란 별(`⭐`) 아이콘으로 표시되며, 메뉴 `7`번(`show_favorites()`)을 누르면 즐겨찾기된 항목들만 따로 모아서 조회할 수 있습니다.

---

## Q10. 브랜치 생성 및 병합 기록이 있는가?
**답변:**
* **확인 방법:** 터미널에서 `git log --oneline --graph` 명령어를 실행합니다.
* **기록:**
  ```text
  *   de160a0 (HEAD -> main, origin/main) merge: feature branch 'bonus-features' 병합...
  |\  
  | * 988a723 (bonus-features) docs: README.md 보너스 기능 및 사용 설명 최신화
  | * f14108d feat: 기본 프롬프트 JSON 영속화 데이터 파일 추가
  | * f9da885 feat: 필수 기능 예외 처리 강화 및 보너스 기능 구현
  | * 0defbaa chore: .gitignore 추가, guide.md 추적 및 test.py 정리
  |/  
  * b9f6ccc Create README.md
  ```
* **설명:** `bonus-features` 브랜치를 새로 만들어 독립적으로 기능을 구현 및 커밋한 뒤, `main` 브랜치로 전환하여 `git merge --no-ff` 명령으로 병합(Merge)했습니다. 그래프 상에서 갈라졌다가 합쳐지는 분기-머지 선이 선명하게 남아 있습니다.

---

## Q11. README가 작성되어 있는가?
**답변:**
* **작성 상태:** 프로젝트 루트의 `README.md`에 마크다운 형식으로 완벽하게 정리되어 있습니다.
* **포함 내용:**
  1. 프로그램 개요 및 목적
  2. Python 실행 방법 (`python3 prompt_manager.py`)
  3. 기본 기능 9가지 및 보너스 기능(JSON 영속화, Markdown 내보내기, 인기 Top 랭킹) 상세 설명
  4. 등록된 6개 카테고리 안내
  5. 기술 스택 및 주요 문법 설명

---
---

# 항목 2. 코드 구조 및 설계

## Q1. 기능별로 함수를 분리한 기준은?
**답변:**
* **기준:** 소프트웨어 공학의 **"단일 책임 원칙 (Single Responsibility Principle)"**을 기준으로 분리했습니다. 즉, **"하나의 함수는 하나의 역할만 완벽히 수행한다"**는 원칙입니다.
* **역할별 분리 구조:**
  * **화면 출력 담당:** `show_menu()` (메뉴 띄우기)
  * **조회/출력 담당:** `show_list()` (전체 목록), `show_by_category()` (카테고리별), `search_prompt()` (검색), `view_detail()` (상세보기), `show_favorites()` (즐겨찾기 목록), `show_top_prompts()` (인기 목록)
  * **데이터 변경 담당:** `add_prompt()` (추가), `edit_prompt()` (수정), `delete_prompt()` (삭제), `toggle_favorite()` (즐겨찾기 토글)
  * **파일 입출력 담당:** `load_prompts()` (JSON 로드), `save_prompts()` (JSON 저장), `export_to_markdown()` (MD 내보내기)
  * **전체 흐름 제어:** `main()` (사용자 입력을 받아 해당 함수로 연결)
* **장점:** 코드가 엉키지 않고 깔끔해지며, 특정 기능에 문제가 생겼을 때 해당 함수만 찾아가서 고칠 수 있어 유지보수가 매우 쉬워집니다.

---

## Q2. 데이터 구조는?
**답변:**
* **구조:** **리스트(List) 안에 딕셔너리(Dictionary)**가 들어있는 중첩 구조입니다: `prompts = [ {...}, {...} ]`
* **개별 프롬프트(딕셔너리)의 속성 구성:**
  ```python
  {
      "title": "블로그 글 작성 도우미",          # 제목 (str)
      "content": "당신은 전문 블로거입니다...",      # 내용 본문 (str)
      "category": "텍스트 생성",                 # 카테고리 (str)
      "favorite": False,                       # 즐겨찾기 여부 (bool)
      "views": 0                               # 누적 조회수 (int, 보너스)
  }
  ```
* **선택 이유:**
  * 프롬프트 하나의 속성들은 키-값(Key-Value) 형태인 **딕셔너리**로 묶어 명확한 이름(`title`, `views` 등)으로 접근합니다.
  * 여러 개의 프롬프트는 순서가 있고 인덱스 번호(1번, 2번...)로 다룰 수 있는 **리스트**에 담아 추가, 삭제, 순회 출력을 편리하게 처리했습니다.

---

## Q3. 입력 검증은 어디에 있는가?
**답변:**
* 사용자가 잘못된 입력을 하더라도 프로그램이 비정상 종료(크래시)되지 않도록 모든 사용자 입력 지점에 **방어 코드(Validation)**를 꼼꼼하게 배치했습니다.
1. **메인 메뉴 (`main`):** 사용자가 0~11 이외의 문자나 엉뚱한 숫자를 넣으면 `else` 문에서 에러 메시지를 띄우고 다시 입력을 유도합니다.
2. **프롬프트 추가 (`add_prompt`):** 제목과 내용에 아무것도 입력하지 않고 엔터만 치는 경우를 막기 위해 `while True`와 `strip()`을 적용하여 유효한 텍스트가 들어올 때까지 재입력을 요청합니다.
3. **번호 선택 함수들 (`view_detail`, `edit_prompt`, `delete_prompt` 등):** `choice.isdigit()`으로 숫자인지 먼저 확인하고, `1 <= int(choice) <= len(prompts)`로 등록된 프롬프트 번호 범위 안에 있는지 검사하여 `IndexError`나 `ValueError`를 완벽히 차단했습니다.

---

## Q4. 커밋을 기능 단위로 나눈 기준은?
**답변:**
* **기준:** **"하나의 커밋은 하나의 명확한 작업 단위(목적)만 담는다"**는 원칙으로 나누었습니다.
* **실제 커밋 내역과 이유:**
  1. `chore: .gitignore 추가, guide.md 추적 및 test.py 정리`: 환경 설정 및 파일 정리 작업만 묶음.
  2. `feat: 필수 기능 예외 처리 강화 및 보너스 기능 구현`: 코드 핵심 기능 개발 작업을 묶음.
  3. `feat: 기본 프롬프트 JSON 영속화 데이터 파일 추가`: 데이터 영속화 파일 반영.
  4. `docs: README.md 보너스 기능 및 사용 설명 최신화`: 문서 작성 작업만 묶음.
  5. `merge: ...`: 브랜치 병합 작업.
* **장점:** 여러 작업이 뒤섞이지 않아 Git 히스토리를 보고 어떤 작업이 언제 이루어졌는지 한눈에 알 수 있고, 문제가 생겼을 때 해당 작업 단위만 안전하게 되돌릴 수 있습니다.

---
---

# 항목 3. 핵심 기술 원리

## Q1. 리스트와 딕셔너리를 선택한 이유?
**답변:**
* **딕셔너리를 선택한 이유:**
  * 프롬프트 하나에는 제목, 내용, 카테고리, 즐겨찾기, 조회수 등 다양한 성격의 정보가 들어갑니다.
  * 딕셔너리를 사용하면 `p["title"]`, `p["views"]`처럼 사람이 이해하기 쉬운 단어(Key)로 데이터를 직관적으로 읽고 쓸 수 있습니다.
* **리스트를 선택한 이유:**
  * 프롬프트 목록은 순서가 중요합니다. "1번 프롬프트, 2번 프롬프트"처럼 순번을 매겨 터미널에 보여주기에 리스트가 가장 이상적입니다.
  * 새로운 프롬프트 추가(`append`), 삭제(`pop`), 정렬(`sorted`) 함수를 기본으로 지원하여 데이터 관리가 매우 간결해집니다.
* **JSON 호환성:**
  * 파이썬의 리스트와 딕셔너리는 파일 저장 포맷인 **JSON 구조(`[...]`, `{...}`)와 100% 동일**하게 일치하므로, 별도의 복잡한 변환 없이 `json.dump()` 한 줄로 파일에 바로 저장하고 불러올 수 있습니다.

---

## Q2. 왜 `while`을 사용했나?
**답변:**
* **1) 프로그램 지속 실행 (메인 루프):**
  * 콘솔 프로그램은 한 가지 기능을 수행했다고 해서 꺼지면 안 됩니다. 사용자가 직접 `0번(종료)`을 누르기 전까지는 계속해서 메뉴를 보여주고 명령을 기다려야 하므로 `while True:` 무한 반복문을 사용했습니다.
* **2) 올바른 입력 유도 (재입력 루프):**
  * 프롬프트를 추가할 때 제목이나 내용을 빈칸으로 두거나, 카테고리 번호를 잘못 입력했을 때 프로그램을 중단시키는 대신 **올바른 값을 입력할 때까지 계속 다시 물어보기 위해** `while`문을 사용했습니다.

---

## Q3. 검색 기능의 원리는?
**답변:**
* **원리:** 파이썬의 **문자열 포함 연산자 `in`**과 **리스트 컴프리헨션(List Comprehension)**을 조합하여 구현했습니다.
* **핵심 코드 로직:**
  ```python
  results = [p for p in prompts if keyword in p["title"] or keyword in p["content"]]
  ```
* **동작 방식:**
  1. 전체 프롬프트 리스트를 처음부터 끝까지 하나씩 순회합니다.
  2. 사용자가 입력한 `keyword`가 프롬프트의 제목(`p["title"]`) 또는 내용(`p["content"]`) 안에 글자 그대로 포함되어 있는지(`in`) 참/거짓을 판별합니다.
  3. 조건이 참(`True`)인 프롬프트들만 모아서 새로운 리스트(`results`)를 만들어 화면에 출력합니다.
  4. 대소문자나 특정 단어가 문장 속 어디에 있더라도(부분 일치) 빠짐없이 찾아냅니다.

---

## Q4. 왜 브랜치를 사용했나?
**답변:**
* **이유:** **"기존의 안정적인 코드를 보호하면서 안전하게 새로운 기능을 개발하기 위해서"**입니다.
* **실제 활용:**
  * 기본 기능이 작동하는 메인 줄기인 `main` 브랜치를 그대로 둔 상태에서,
  * `bonus-features`라는 별도의 가지(브랜치)를 뻗어나와 보너스 기능 구현과 리팩토링을 진행했습니다.
  * 이렇게 하면 개발 도중에 코드가 망가지거나 오류가 생겨도 `main` 브랜치에는 전혀 영향이 없습니다.
  * 작업이 완벽하게 끝나고 테스트까지 통과한 후에 비로소 `main` 브랜치로 안전하게 합치는(Merge) 방식을 사용했습니다. 이것이 실무 소프트웨어 개발의 표준 협업 방식입니다.

---
---

# 항목 4. 심층 인터뷰

## Q1. 프로그램 종료 후에도 데이터가 유지되려면?
**답변:**
* **원리:** 컴퓨터가 켜져 있는 동안 실행되는 프로그램의 변수들은 휘발성 메모리(RAM)에 저장되므로, 프로그램을 끄면 사라집니다. 영구히 유지하려면 **하드디스크/SSD 같은 저장장치에 파일 형태로 기록(영속화, Persistence)**해야 합니다.
* **구현한 방식 (보너스 1):**
  * 파이썬 내장 `json` 모듈을 활용하여 [`prompts.json`](file:///Users/jekim29842984/Desktop/A1-1/prompts.json) 파일에 데이터를 저장하고 불러오는 기능을 완성했습니다.
  * 프로그램 시작 시 `load_prompts()`가 실행되어 파일에서 기존 데이터를 읽어오고,
  * 프롬프트를 추가, 수정, 삭제하거나 즐겨찾기를 바꾸고 상세보기를 조회할 때마다 `save_prompts()`가 자동으로 파일에 최신 상태를 덮어씁니다.
* **확장 방안:** 만약 데이터가 수만 건 이상으로 많아진다면 경량 데이터베이스인 **SQLite**나 **PostgreSQL** 같은 DBMS를 연동하여 관리할 수 있습니다.

---

## Q2. 같은 제목의 프롬프트가 여러 개 생긴다면?
**답변:**
* **현재 동작:**
  * 우리 프로그램은 프롬프트를 식별할 때 '제목'이 아니라 리스트의 **인덱스 번호(1번, 2번, 3번...)**를 기준으로 다루기 때문에, 제목이 중복되더라도 프로그램이 멈추거나 오작동하지 않고 정상 작동합니다.
* **더 좋은 개선 방안 (고도화):**
  1. **중복 방지 유효성 검사:** `add_prompt()` 실행 시 이미 등록된 제목인지 검사(`any(p["title"] == title for p in prompts)`)하여, 이미 있는 제목이면 "동일한 제목의 프롬프트가 이미 존재합니다." 경고를 주고 등록을 막거나 변경을 유도할 수 있습니다.
  2. **고유 ID(Primary Key) 부여:** 실무 데이터베이스처럼 각 프롬프트마다 고유한 일련번호(`id: 1, 2, 3...`) 또는 `uuid`를 부여하여 제목이 같더라도 고유 식별자로 명확하게 구분할 수 있습니다.

---

## Q3. Merge conflict(병합 충돌)가 발생하면?
**답변:**
* **발생 원인:** 서로 다른 두 브랜치에서 **동일한 파일의 동일한 줄(코드 라인)**을 서로 다르게 수정한 상태에서 하나로 합치려고(`merge`) 할 때, Git이 어느 쪽 코드가 맞는지 스스로 결정할 수 없어 충돌을 일으킵니다.
* **해결 절차 (3단계):**
  1. **충돌 파일 확인:** 충돌이 난 파일(예: `prompt_manager.py`)을 열어보면 Git이 충돌 지점을 특수 기호로 표시해 둡니다:
     ```text
     <<<<<<< HEAD (현재 브랜치 코드)
     print("메뉴 1")
     =======
     print("메뉴 2")
     >>>>>>> bonus-features (합쳐지려는 브랜치 코드)
     ```
  2. **코드 수동 조율:** 개발자가 직접 코드를 검토하여 둘 중 맞는 코드를 선택하거나 두 코드를 적절히 합쳐서 작성한 뒤, 특수 기호(`<<<<<<<`, `=======`, `>>>>>>>`)를 모두 지웁니다.
  3. **충돌 해결 커밋:** 수정한 파일을 `git add <파일명>`으로 스테이징하고, `git commit`을 실행하면 충돌이 깔끔하게 해결된 머지 커밋이 완성됩니다.

---

## Q4. 카테고리가 늘어나면 어디를 수정해야 하나?
**답변:**
* **수정할 위치:** [`prompt_manager.py`](file:///Users/jekim29842984/Desktop/A1-1/prompt_manager.py) 코드 최상단에 정의된 전역 리스트 **`CATEGORIES` 딱 한 곳만 수정**하면 됩니다!
* **코드 예시:**
  ```python
  CATEGORIES = [
      "텍스트 생성",
      "이미지 생성",
      "영상 생성",
      "페르소나",
      "자동화",
      "코딩/개발",   # <-- 여기에 원하는 새 카테고리를 한 줄만 추가!
      "기타"
  ]
  ```
* **이유 (유연한 코드 설계):**
  * 우리 프로그램은 카테고리 개수를 고정(하드코딩)하지 않고, `len(CATEGORIES)`와 `for cat in CATEGORIES:` 반복문을 사용해 동적으로 처리하도록 설계되었습니다.
  * 따라서 리스트에 항목을 추가하기만 하면 **메뉴 출력(1~N번 선택), 카테고리별 조회, 마크다운 내보내기 목차 생성까지 모든 기능에 자동으로 즉시 반영**되므로 다른 함수 코드를 전혀 수정할 필요가 없습니다.